def longest_word(words):
    longest = words[0]
    for word in words:
        if len(word) > len(longest):
            longest = word
    return longest
print(longest_word(["boy","girl","aws","coconut"]))
print(longest_word(["chicken","aviram","afik","dean"]))
